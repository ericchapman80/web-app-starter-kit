# Agent contract

Read `docs/adr/` before proposing infrastructure changes. Keep product-specific behavior out of the M0 foundation. Never commit secrets or real environment values. Before handing off a change, run `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build` when dependencies are installed. Humans own intent, architecture approval, and merging.
